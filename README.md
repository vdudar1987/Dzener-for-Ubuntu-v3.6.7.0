# Dzener

## 📌 О проекте

**Dzener** — набор инструментов для автоматизации активности вокруг публикаций и видео в Яндекс Дзен.

В репозитории доступны:

- **Windows-сборка** (`windows/`) — исходный комплект исполняемых файлов;
- **Linux Edition** (`linux_app/`) — актуализированная Python-реализация (`v3.7.0`) с:
  - консольным режимом;
  - графическим интерфейсом (**Tkinter**);
  - сохранением состояния и экспортом задач.

---

## 🚀 Что умеет Dzener Linux Edition

- 📖 увеличивать количество дочитываний публикаций;
- 🎬 увеличивать просмотры видео;
- 👍/👎 выполнять реакции (лайк/дизлайк);
- 👥 добавлять подписки;
- 🔖 добавлять сохранения в закладки;
- 💬 работать с комментариями;
- 🧾 экспортировать список собственных задач в JSON;
- 🛠️ удалять свои задачи и сбрасывать локальную статистику.

---

## ⚙️ Принцип работы

Сервис использует механику **взаимного обмена действиями**:

1. Пользователь добавляет ссылку на материал.
2. Материал попадает в очередь задач.
3. Клиент выполняет задания других участников.
4. За каждое выполненное задание начисляются баллы (в Linux-версии — `+2` за задачу).

---

## 🐧 Linux Edition

Основной файл: `linux_app/dzener_linux.py`.

Локальные данные:

- `~/.dzener-linux/state.json` — состояние пользователя;
- `~/.dzener-linux/tasks_export.json` — экспорт ваших задач.

### Запуск

```bash
# консольный режим
python3 linux_app/dzener_linux.py

# графический режим (Tkinter GUI)
python3 linux_app/dzener_linux.py --gui

# пакетная обработка заданий (без интерактива)
python3 linux_app/dzener_linux.py --process 10

# версия
python3 linux_app/dzener_linux.py --version
```

---

## 📋 Требования

- Аккаунт в Яндекс Дзен;
- Python 3.10+ для Linux-версии;
- для GUI-режима в Linux — установленный Tkinter.

---

## ⚠️ Важно

Использование подобных инструментов может нарушать правила платформы Яндекс Дзен.
Пользователь самостоятельно несёт ответственность за возможные последствия использования.
